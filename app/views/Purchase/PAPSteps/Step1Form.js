import React from 'react';
import {
    Form, Icon, Input, Button, Select 
} from 'antd';

const FormItem = Form.Item;
const Option = Select.Option;
import {} from '../../../axios/findAPI'

export default class Step1Form extends React.Component {
    constructor(props){
        super(props);//	必须调用超类
    }
    render() {
        return (
            <div>
                fdsfasdsdss
            </div>
        )
    }
}
